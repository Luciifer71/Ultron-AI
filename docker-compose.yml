services:
  nats:
    image: nats:2.10-alpine
    container_name: nats
    restart: unless-stopped
    ports:
      - "4222:4222"
      - "8222:8222"
    command: "-js -sd /data --http_port 8222"
    volumes:
      - ./nats_data:/data